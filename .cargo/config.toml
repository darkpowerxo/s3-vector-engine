# Enable native CPU features for SIMD auto-vectorization.
# The cosine similarity inner loop benefits from AVX2/NEON.

[target.'cfg(target_arch = "x86_64")']
rustflags = ["-C", "target-cpu=native"]

[target.'cfg(target_arch = "aarch64")']
rustflags = ["-C", "target-cpu=native"]
